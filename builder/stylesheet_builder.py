from builder import results_printer

# We want all local stylesheets to become one application.css file
def build_stylesheets():
  success = False
  # code here

  return results_printer.print_result('Stylesheets', 'built', success)
